-r requirements.txt
# Orchestration (optional at runtime, exercised in tests)
dagster>=1.10
dagster-webserver>=1.10
# Quality
pytest>=8.0
ruff>=0.6
# Notebook
jupyter>=1.0
nbconvert>=7.0
matplotlib>=3.8
